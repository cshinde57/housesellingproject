<h1>--------Editing Inquiry------------</h1>

<br>
<% if @inquiry.errors.any? %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(@inquiry.errors.count, "error") %> prohibited
      this article from being saved:
    </h2>
    <ul>
      <% @inquiry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
<br>
<%= form_with url: "/inquiries/"+(@inquiry.id).to_s,  scope: :inquiry , method: 'PATCH' , local: true do |form| %>

  <%= form.label :subject %>
  <%= form.text_field :subject %>
  <br>
  <br>
  <%= form.label :message_content %>
  <%= form.text_area :message_content %>
  <br>
  <br>

  <br>


  <br>
  <%= submit_tag("Update") %>
<% end %>
<br>
<br>
<%= link_to 'Go back to homepage ', homepage_path %>